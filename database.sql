CREATE database 'project'
use project
CREATE Table 'property'{
    'pid' int(10) NOT NULL PRIMARY KEY,
    'propname' VARCHAR(100) NOT NULL,
    'member_name' VARCHAR(100),
    'member_phno' VARCHAR(100),
    'member_email' VARCHAR(100),
}